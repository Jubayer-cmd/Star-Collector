export interface ButtonConfig {
    buttonText: string;
    buttonStyle: {
        color: string;
        backgroundColor: string;
        top: string;
        left: string;
        width: string;
        height: string;
        borderRadius: string;
        fontSize: string;
    };
}

